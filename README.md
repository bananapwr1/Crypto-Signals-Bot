# 🤖 Crypto Trading Bot

Автоматизированный торговый бот для бинарных опционов с AI-анализом рынка и интеграцией с Pocket Option.

## 📋 Основные возможности

- **AI Core** - Анализ рынка на основе технического анализа
- **AutoTrader** - Автоматическое выполнение сделок
- **Telegram Bot UI** - Удобный интерфейс для пользователей
- **Admin Panel** - Веб-панель и LLM-чат для администрирования
- **Multi-strategy** - Поддержка различных торговых стратегий (Мартингейл, Д'Аламбер, процентная ставка)
- **Supabase Integration** - Облачная база данных PostgreSQL

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────┐
│           TELEGRAM BOT UI                    │
│  - Команды для пользователей                │
│  - Управление подписками                    │
│  - Настройки автоторговли                   │
└──────────────────┬──────────────────────────┘
                   │
        ┌──────────┴───────────┐
        │                      │
        ↓                      ↓
┌───────────────┐      ┌───────────────┐
│   AI CORE     │      │  AUTOTRADER   │
│               │      │               │
│ - Технический │──────→ - Выполнение  │
│   анализ      │        сделок        │
│ - Генерация   │      │ - Стратегии   │
│   сигналов    │      │ - Мани-       │
│               │      │   менеджмент  │
└───────┬───────┘      └───────┬───────┘
        │                      │
        └──────────┬───────────┘
                   ↓
        ┌──────────────────────┐
        │   SUPABASE БД        │
        │  - Пользователи      │
        │  - Сигналы           │
        │  - Сделки            │
        │  - Статистика        │
        └──────────────────────┘
```

## 📦 Структура проекта

### Основные файлы
- `main.py` - Точка входа приложения
- `config.py` - Конфигурация и настройки
- `db_manager.py` - Менеджер базы данных (Supabase)

### Торговые модули
- `ai_core.py` - AI анализ рынка и генерация сигналов
- `autotrader.py` - Автоматическое выполнение сделок
- `pocket_option_api.py` - API для Pocket Option

### Пользовательский интерфейс
- `ui_handlers.py` - Обработчики команд пользователей
- `admin_manager.py` - Админ-панель с LLM-чатом
- `app.py` - Flask веб-панель для мониторинга

### Утилиты
- `crypto_utils.py` - Шифрование и безопасность
- `webhook_system.py` - Система вебхуков

## 🚀 Развертывание на Bothost

### 1. Подготовка репозитория

Убедитесь, что у вас есть:
- `requirements.txt` с минимальным набором зависимостей
- `main.py` - точка входа
- `.env.example` с примерами переменных окружения

### 2. Регистрация на Bothost

1. Перейдите на https://bothost.ru
2. Зарегистрируйте аккаунт
3. Создайте новый бот

### 3. Настройка переменных окружения

В панели Bothost добавьте следующие переменные:

```env
TELEGRAM_TOKEN=ваш_токен_telegram_бота
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_supabase_anон_ключ
POCKET_OPTION_SSID=ваш_pocket_option_ssid
ANTHROPIC_API_KEY=ваш_anthropic_api_ключ
ADMIN_IDS=123456789,987654321
```

### 4. Подключение репозитория

1. В панели Bothost укажите URL вашего GitHub репозитория
2. Bothost автоматически:
   - Склонирует репозиторий
   - Установит зависимости из `requirements.txt`
   - Запустит `main.py`

### 5. Запуск бота

После настройки нажмите "Запустить" в панели Bothost. Бот начнет работу и запустит три параллельных потока:

1. **Telegram Bot UI** - Обработка команд пользователей
2. **AI Core** - Анализ рынка и генерация сигналов
3. **AutoTrader** - Выполнение сделок

## 🔧 Локальная разработка

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка переменных окружения

Создайте файл `.env`:

```bash
cp .env.example .env
```

Заполните необходимые переменные:

```env
TELEGRAM_TOKEN=your_telegram_bot_token
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
POCKET_OPTION_SSID=your_pocket_option_ssid
ANTHROPIC_API_KEY=your_anthropic_api_key
ADMIN_IDS=123456789,987654321
```

### Запуск

```bash
python main.py
```

## 📊 База данных (Supabase)

### Таблица `users`
```sql
- user_id (text, PK)
- username (text)
- subscription_type (text)
- subscription_end (timestamp)
- auto_trading_enabled (boolean)
- trading_strategy (text)
- current_balance (numeric)
```

### Таблица `signals`
```sql
- id (bigint, PK)
- symbol (text)
- signal_type (text) -- CALL/PUT
- source (text) -- ai_core/parser/external
- confidence (numeric)
- entry_price (numeric)
- created_at (timestamp)
```

### Таблица `trades`
```sql
- id (bigint, PK)
- user_id (text, FK)
- signal_id (bigint, FK)
- amount (numeric)
- result (text) -- win/loss/pending
- profit_loss (numeric)
- created_at (timestamp)
```

## 🎯 Команды бота

### Команды для пользователей

- `/start` - Главное меню
- `/plans` - Тарифы и подписки
- `/bank` - Управление балансом
- `/autotrade` - Настройки автоторговли
- `/settings` - Настройки профиля
- `/short` - Получить SHORT сигнал
- `/long` - Получить LONG сигнал
- `/my_longs` - Мои активные позиции
- `/my_stats` - Статистика сделок
- `/help` - Помощь

### Команды для администраторов

- `/manager` - Админ-панель с управлением
- `/stats` - Глобальная статистика системы
- `/logs` - Просмотр логов
- **Текстовые сообщения** - LLM-чат для управления ботом

## 🧠 AI Core

AI Core использует технический анализ для генерации торговых сигналов:

### Индикаторы
- **RSI** (Relative Strength Index)
- **MACD** (Moving Average Convergence Divergence)
- **Bollinger Bands**
- **EMA** (Exponential Moving Average)
- **Stochastic Oscillator**

### Режимы работы
- **Автономный анализ** - Постоянный мониторинг рынка
- **По запросу** - Анализ конкретных пар по команде

## 💼 Торговые стратегии

### 1. Процентная ставка (Percentage)
```
Ставка = Баланс × Процент
```

### 2. Мартингейл (Martingale)
```
Ставка = Базовая ставка × (Множитель ^ Уровень убытков)
```

### 3. Д'Аламбер (D'Alembert)
```
Ставка = Базовая ставка + (Единица × Уровень)
```

## 🔐 Безопасность

- ✅ Шифрование SSID через Fernet
- ✅ Безопасное хранение credentials в переменных окружения
- ✅ Проверка прав администратора
- ✅ Rate limiting для API запросов
- ✅ Валидация входных данных

## 📈 Мониторинг

### Flask Web Panel
Веб-панель доступна для мониторинга системы:

- Статистика пользователей
- История команд
- Системные метрики
- Health check endpoints

### Логирование
Все события записываются в `bot.log` с детальной информацией:

```python
logger.info("✅ Успешная операция")
logger.warning("⚠️ Предупреждение")
logger.error("❌ Ошибка")
```

## 🔧 Конфигурация

### Торговые параметры
```python
# config.py
INITIAL_BALANCE = 1000.0
MIN_TRADE_AMOUNT = 1.0
MAX_TRADE_AMOUNT = 10000.0
DEFAULT_PERCENTAGE = 2.5
MARTINGALE_MULTIPLIER = 3
```

### Параметры AI
```python
# ai_core.py
ANALYSIS_INTERVAL = 300  # 5 минут между анализами
MIN_CONFIDENCE = 40  # Минимальная уверенность для сигнала (%)
```

## 🐛 Troubleshooting

### Бот не запускается
**Проблема:** Ошибка при старте  
**Решение:** Проверьте переменные окружения в `.env` или в панели Bothost

### Ошибка подключения к БД
**Проблема:** Не удается подключиться к Supabase  
**Решение:** Убедитесь, что `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` корректны

### Сделки не выполняются
**Проблема:** AutoTrader не открывает позиции  
**Решение:** Проверьте валидность `POCKET_OPTION_SSID` (токен может устареть)

### Ошибка apt-key при сборке
**Проблема:** `apt-key add` возвращает ошибку 127  
**Решение:** Удалите все Dockerfile из репозитория, Bothost использует автоматическую сборку

## 📝 Зависимости

Минимальный набор для работы на Bothost:

```txt
python-telegram-bot==20.7
requests==2.31.0
python-dotenv==1.0.0
supabase==2.3.0
```

Полный список см. в `requirements.txt`

## 🔄 Обновление

### На Bothost
1. Внесите изменения в GitHub репозиторий
2. В панели Bothost нажмите "🔄 Обновить"
3. Bothost автоматически пересоберет бота

### Локально
```bash
git pull origin main
pip install -r requirements.txt --upgrade
python main.py
```

## 📧 Поддержка

Для вопросов и помощи:
- Создайте issue в репозитории
- Свяжитесь с администратором бота

---

**Версия:** 2.0  
**Дата:** 2025-12-10  
**Статус:** Production Ready ✅  
**Платформа:** Bothost Compatible 🚀
