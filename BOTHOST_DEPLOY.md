# 🚀 Инструкция по деплою на Bothost

## ✅ Проблема решена

**Ошибка:** `apt-key: not found` при установке Google Chrome  
**Причина:** Устаревший метод добавления ключей APT  
**Решение:** Создан новый Dockerfile с современным методом установки Chrome

## 📋 Что было сделано

1. ✅ Создан `Dockerfile` с правильной установкой Chrome (через `/etc/apt/keyrings/`)
2. ✅ Создан `.dockerignore` для оптимизации сборки образа
3. ✅ Создан `docker-compose.yml` для локального тестирования

## 🔄 Как задеплоить бот

### Шаг 1: Закоммитить изменения

```bash
git add Dockerfile .dockerignore docker-compose.yml BOTHOST_DEPLOY.md
git commit -m "fix: обновлен Dockerfile для Bothost (исправлена ошибка apt-key)"
git push origin main
```

### Шаг 2: Автоматическое обновление

Bothost автоматически обнаружит изменения через webhook:
- 🔗 Webhook URL: `http://agent.bothost.ru/api/webhooks/github`
- ⚙️ Автообновление должно быть настроено в настройках бота

### Шаг 3: Проверка сборки

1. Зайдите в панель Bothost
2. Откройте бот `Crypto-Signals-Bot`
3. Перейдите во вкладку "🔨 Логи сборки"
4. Дождитесь успешной сборки образа

## ✨ Что изменилось в Dockerfile

### ❌ Старый метод (не работает):
```bash
apt-key add -
echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list
```

### ✅ Новый метод (работает):
```bash
mkdir -p /etc/apt/keyrings
wget -q -O /tmp/google-chrome-key.pub https://dl-ssl.google.com/linux/linux_signing_key.pub
gpg --dearmor -o /etc/apt/keyrings/google-chrome.gpg /tmp/google-chrome-key.pub
echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/google-chrome.gpg] http://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources.list.d/google-chrome.list
```

## 🧪 Локальное тестирование

Если хотите протестировать сборку локально:

```bash
# Сборка образа
docker build -t crypto-signals-bot .

# Запуск через docker-compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 🐛 Возможные проблемы

### Проблема: Бот не запускается после сборки

**Решение:**
1. Проверьте, что файл `.env` загружен в Bothost
2. Убедитесь, что все переменные окружения заданы:
   - `TELEGRAM_BOT_TOKEN`
   - `SUPABASE_URL`
   - `SUPABASE_KEY`
   - И другие необходимые переменные

### Проблема: Ошибка при установке зависимостей

**Решение:**
1. Проверьте `requirements.txt` на наличие конфликтов версий
2. Попробуйте обновить версии пакетов

### Проблема: Недостаточно памяти

**Решение:**
1. Оптимизируйте использование памяти в коде
2. Обратитесь в поддержку Bothost для увеличения лимитов (у вас тариф Pro)

## 📞 Поддержка

- 💬 Telegram-группа Bothost
- 📧 Email: support@bothost.ru
- 🎫 Тикеты поддержки в панели Bothost

## 📝 Примечания

- Используется Python 3.11
- Base image: `python:3.11-slim-bookworm`
- Chrome устанавливается для работы Selenium (SSID extraction)
- Порт по умолчанию: 8080 (для webhook)

---

🎉 **Готово!** После пуша изменений бот должен успешно собраться и запуститься на Bothost.
