# 🚀 Развертывание на BotHost - Облегченная версия

## 📋 Обзор

Этот проект теперь поддерживает **два режима развертывания**:

### 1. **BotHost (Облегченная версия)** ⚡
- ✅ Только Telegram интерфейс
- ✅ Базовые функции бота
- ✅ Работа с Supabase БД
- ✅ LLM-чат для админов
- ❌ БЕЗ Chrome/Selenium
- ❌ БЕЗ pandas/numpy/matplotlib
- ❌ БЕЗ автоматического анализа рынка

### 2. **Полная версия (Локально/PythonAnywhere)** 🔥
- ✅ Все функции BotHost
- ✅ Chrome + Selenium для SSID extraction
- ✅ Pandas/NumPy для анализа данных
- ✅ Автоматический анализ рынка через yfinance
- ✅ Генерация графиков

---

## 🎯 Развертывание на BotHost

### Шаг 1: Подготовка файлов

BotHost использует **облегченный Dockerfile** без тяжелых зависимостей:

```bash
# Убедитесь, что у вас есть эти файлы:
- Dockerfile.bothost        # Облегченный Dockerfile
- requirements-bothost.txt  # Минимальные зависимости
- main.py                   # Точка входа
- .env                      # Переменные окружения
```

### Шаг 2: Создание .env файла

```env
# Telegram
TELEGRAM_TOKEN=your_telegram_bot_token

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key

# Anthropic (опционально, для LLM-чата с админами)
ANTHROPIC_API_KEY=your_anthropic_key

# Админы (через запятую)
ADMIN_IDS=123456789,987654321

# Flask (если используется)
FLASK_SECRET_KEY=your_random_secret_key
FLASK_HOST=0.0.0.0
FLASK_PORT=8080
```

### Шаг 3: Настройка BotHost

1. **Создайте новый проект** на BotHost
2. **Загрузите файлы** через Git или ZIP
3. **Укажите Dockerfile**: `Dockerfile.bothost`
4. **Добавьте переменные окружения** из .env
5. **Запустите сборку**

### Шаг 4: Проверка сборки

BotHost должен успешно собрать Docker контейнер:

```bash
# Лог сборки должен показывать:
✅ Successfully installed python-telegram-bot-20.7
✅ Successfully installed supabase-2.3.1
✅ Successfully installed anthropic-0.34.0
...
✅ Successfully built
```

### Шаг 5: Запуск бота

После успешной сборки бот автоматически запустится:

```bash
# Проверьте логи:
🚀 ЗАПУСК МОНОЛИТНОГО СЕРВИСА
✅ Конфигурация валидна
📊 Инициализация Database Manager...
💰 Инициализация Pocket Option API...
⚠️  AI Core работает в облегченном режиме (pandas/yfinance недоступны)
📱 Инициализация UI Handlers...
✅ Все компоненты инициализированы
```

---

## ⚙️ Конфигурация для BotHost

### Используемые файлы

| Файл | Назначение |
|------|-----------|
| `Dockerfile.bothost` | Облегченный Docker образ без Chrome |
| `requirements-bothost.txt` | Только необходимые зависимости (~50MB) |
| `main.py` | Точка входа (автоматически определяет режим) |

### Основные зависимости

```txt
python-telegram-bot==20.7  # Telegram Bot API
supabase==2.3.1           # База данных
anthropic==0.34.0         # LLM для админ-чата
ta==0.11.0                # Легковесный технический анализ
Flask==3.0.0              # Веб-сервер для webhook
```

**Размер образа**: ~300-400MB (вместо 1.5GB с Chrome)

---

## 🔧 Что работает в облегченной версии

### ✅ Доступные функции:

1. **Telegram интерфейс**
   - `/start` - Главное меню
   - `/plans` - Тарифные планы
   - `/bank` - Управление банком
   - `/autotrade` - Автоторговля
   - `/settings` - Настройки

2. **Автоторговля**
   - Подключение к Pocket Option через SSID
   - Стратегии: Мартингейл, Процентная, Д'Аламбер
   - Demo/Real режимы

3. **Админ-панель**
   - `/manager` - Панель управления
   - `/stats` - Статистика
   - LLM-чат с Claude

4. **База данных**
   - Supabase для хранения данных
   - Пользователи, сигналы, сделки

### ❌ Недоступные функции:

1. **Анализ рынка**
   - ~~Автоматический сбор данных через yfinance~~
   - ~~Технический анализ с pandas/numpy~~
   - ~~Генерация графиков~~

2. **SSID extraction**
   - ~~Автоматическое извлечение SSID через Selenium~~
   - ~~Работа с Chrome WebDriver~~

**Решение**: Используйте внешний сервис для анализа (например, PythonAnywhere)

---

## 🐛 Устранение проблем

### Проблема: Ошибка `apt-key: not found`

**Причина**: Используется старый Dockerfile с устаревшей командой `apt-key`

**Решение**: Убедитесь, что BotHost использует `Dockerfile.bothost`:

```bash
# В настройках BotHost укажите:
Dockerfile: Dockerfile.bothost
```

### Проблема: Ошибка `ModuleNotFoundError: No module named 'pandas'`

**Причина**: Код пытается импортировать pandas, который недоступен в облегченной версии

**Решение**: Обновите код до последней версии. В `ai_core.py` все импорты pandas теперь опциональные:

```python
try:
    import pandas as pd
    import numpy as np
    PANDAS_AVAILABLE = True
except ImportError:
    PANDAS_AVAILABLE = False
    pd = None
    np = None
```

### Проблема: Бот не отвечает на команды

**Возможные причины**:
1. Неправильный `TELEGRAM_TOKEN`
2. Неправильные `ADMIN_IDS`
3. Проблемы с Supabase

**Решение**:
```bash
# Проверьте логи BotHost:
# 1. Убедитесь, что бот запустился
# 2. Проверьте переменные окружения
# 3. Проверьте подключение к Supabase
```

### Проблема: Сборка занимает слишком много времени

**Причина**: BotHost использует полный `requirements.txt` вместо облегченного

**Решение**:
```bash
# В Dockerfile.bothost должно быть:
COPY requirements-bothost.txt .
RUN pip install -r requirements-bothost.txt

# НЕ:
COPY requirements.txt .
RUN pip install -r requirements.txt
```

---

## 📊 Сравнение версий

| Параметр | BotHost (облегченная) | Полная версия |
|----------|---------------------|---------------|
| Размер Docker образа | ~400MB | ~1.5GB |
| Время сборки | 3-5 минут | 10-15 минут |
| RAM | 256MB | 1GB+ |
| Chrome/Selenium | ❌ | ✅ |
| Pandas/NumPy | ❌ | ✅ |
| Анализ рынка | ❌ | ✅ |
| Telegram интерфейс | ✅ | ✅ |
| Автоторговля | ✅ | ✅ |

---

## 🚀 Рекомендуемая архитектура

Для полной функциональности используйте **гибридную архитектуру**:

```
┌─────────────────┐         ┌──────────────────┐
│   BotHost       │         │  PythonAnywhere  │
│  (Telegram UI)  │◄────────┤  (Анализ рынка)  │
│                 │         │                  │
│ - Интерфейс     │         │ - yfinance       │
│ - Автоторговля  │         │ - Pandas/NumPy   │
│ - LLM-чат       │         │ - Генерация      │
│                 │         │   сигналов       │
└────────┬────────┘         └────────┬─────────┘
         │                           │
         └───────────┬───────────────┘
                     │
              ┌──────▼────────┐
              │   Supabase    │
              │  (Database)   │
              │               │
              │ - Пользователи│
              │ - Сигналы     │
              │ - Сделки      │
              └───────────────┘
```

### Преимущества:
- ✅ BotHost: Быстрая сборка, низкие требования
- ✅ PythonAnywhere: Полный анализ данных
- ✅ Supabase: Общая база данных
- ✅ Масштабируемость: каждый компонент работает независимо

---

## 📝 Дополнительные инструкции

### Обновление на новую версию

```bash
# 1. Загрузите новые файлы на BotHost
# 2. Перезапустите сборку
# 3. Проверьте логи
```

### Мониторинг

```bash
# Проверка статуса бота:
curl https://your-bothost-url.com/health

# Ответ:
{
  "status": "healthy",
  "database": "available",
  "timestamp": "2025-12-10T10:00:00Z"
}
```

### Backup

```bash
# База данных Supabase автоматически создает backup
# Скачайте .env файл для восстановления настроек
```

---

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи BotHost
2. Убедитесь, что используете `Dockerfile.bothost`
3. Проверьте переменные окружения
4. Убедитесь, что Supabase доступен

---

## ✅ Checklist перед развертыванием

- [ ] Создан Telegram бот через @BotFather
- [ ] Настроен проект Supabase
- [ ] Созданы таблицы в Supabase (users, signals, trades)
- [ ] Файл .env заполнен корректными данными
- [ ] На BotHost указан `Dockerfile.bothost`
- [ ] Переменные окружения добавлены в BotHost
- [ ] Проверены логи после запуска
- [ ] Бот отвечает на команду `/start`

---

**Готово!** 🎉 Ваш бот развернут на BotHost и готов к работе!
