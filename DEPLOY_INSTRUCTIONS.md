# 🚀 Инструкция по деплою монолитного бота

## ✅ Что было создано

### 1. **main.py** - Координатор всех модулей
Единая точка входа, которая:
- ✅ Инициализирует все компоненты (DB, AI Core, AutoTrader, UI)
- ✅ Запускает 3 параллельных асинхронных потока через `asyncio.gather`:
  1. **Telegram Bot UI** - обработка команд пользователей (polling)
  2. **AI Core** - бесконечный цикл аналитики рынка
  3. **AutoTrader** - автоторговля + парсинг Telegram каналов
- ✅ Регистрирует все хэндлеры (клиентские + админские)
- ✅ Поддерживает LLM-чат для админов (через Anthropic Claude)

### 2. **requirements.txt** - Полный список зависимостей
Содержит все необходимые пакеты:
- `python-telegram-bot` - Telegram Bot API
- `supabase` - База данных
- `anthropic` - LLM для админ-чата
- `telethon` - Парсинг Telegram каналов
- `yfinance` - Рыночные данные
- `ta` - Технический анализ
- `selenium` - Автоматизация браузера (SSID)
- И другие (всего 40+ пакетов)

## 📋 Структура проекта

```
/workspace/
├── main.py                 ⭐ ТОЧКА ВХОДА (создан)
├── requirements.txt        ⭐ ЗАВИСИМОСТИ (обновлен)
├── config.py              ✅ Конфигурация
├── db_manager.py          ✅ База данных (Supabase)
├── ai_core.py             ✅ AI аналитика
├── autotrader.py          ✅ Автоторговля + парсинг TG
├── ui_handlers.py         ✅ Клиентские хэндлеры
├── admin_manager.py       ✅ Админ-панель + LLM-чат
├── pocket_option_api.py   ✅ API Pocket Option
├── crypto_utils.py        ✅ Шифрование SSID
├── run_bot.py             ⚠️ Устаревший (используйте main.py)
├── .env.example           📄 Пример переменных окружения
├── Dockerfile             🐳 Docker образ
└── docker-compose.yml     🐳 Docker Compose
```

## 🔧 Переменные окружения

Создайте файл `.env` или настройте в Bothost:

```env
# ============ ОБЯЗАТЕЛЬНЫЕ ============

# Telegram Bot
BOT_TOKEN=your_telegram_bot_token

# Supabase Database
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Администраторы (через запятую)
ADMIN_IDS=7746862973

# ============ ОПЦИОНАЛЬНЫЕ ============

# Anthropic Claude (для LLM-чата с админами)
ANTHROPIC_API_KEY=sk-ant-xxx

# Telegram Client API (для парсинга каналов)
TG_API_ID=your_api_id
TG_API_HASH=your_api_hash
TARGET_CHAT_ID=-1001234567890,-1009876543210

# Encryption (для SSID)
ENCRYPTION_KEY=your_32_byte_encryption_key

# Flask (если используется webhook)
FLASK_SECRET_KEY=your_secret_key
```

## 🚀 Деплой на Bothost

### Шаг 1: Коммит и Push

```bash
# Проверяем изменения
git status

# Добавляем файлы
git add main.py requirements.txt

# Коммитим
git commit -m "feat: добавлен main.py - единая точка входа монолитного сервиса"

# Пушим в main ветку
git push origin main
```

### Шаг 2: Настройка в Bothost

1. Зайдите в панель Bothost
2. Откройте ваш бот `Crypto-Signals-Bot`
3. Проверьте, что автообновление настроено:
   - Webhook: `http://agent.bothost.ru/api/webhooks/github`
4. Установите переменные окружения (см. выше)
5. **ВАЖНО**: Измените команду запуска с `run_bot.py` на `main.py`:
   - В настройках бота укажите: `python main.py`
6. Перезапустите бот

### Шаг 3: Проверка работы

После запуска проверьте логи:

```
📋 Логи работы
```

Вы должны увидеть:

```
🚀 ИНИЦИАЛИЗАЦИЯ КОМПОНЕНТОВ
✅ Database Manager инициализирован
✅ AI Core инициализирован
✅ AutoTrader инициализирован
✅ UIHandlers инициализирован
✅ AdminManager инициализирован
🔄 ЗАПУСК ПАРАЛЛЕЛЬНЫХ ПОТОКОВ
📊 Итерация анализа #1
🤖 Итерация автоторговли #1
```

## 🧪 Локальное тестирование

### Установка зависимостей

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

### Запуск

```bash
# Убедитесь, что .env файл заполнен
python main.py
```

### Остановка

```
Ctrl+C
```

## 📊 Команды бота

### Для клиентов:
- `/start` - Главное меню
- `/plans` - Тарифы и подписки
- `/bank` - Управление банком
- `/autotrade` - Автоторговля (VIP)
- `/short` - SHORT сигнал (1-5 мин)
- `/long` - LONG сигнал (1-4 часа)
- `/my_stats` - Статистика
- `/help` - Помощь

### Для админов:
- `/manager` - Админ-панель
- `/stats` - Статистика бота
- `/logs` - Просмотр логов
- Любое текстовое сообщение - LLM-чат

## 🔄 Как работают параллельные потоки

### Поток 1: Telegram Bot UI
```python
app.run_polling()
```
- Обрабатывает входящие сообщения и команды
- Отвечает пользователям
- Обрабатывает callback кнопки

### Поток 2: AI Core
```python
ai_core.run_analysis_cycle()
```
- Каждые 5 минут анализирует рынок
- Получает данные через yfinance
- Рассчитывает технические индикаторы (RSI, MACD, Bollinger Bands)
- Генерирует торговые сигналы
- Сохраняет сигналы в БД

### Поток 3: AutoTrader
```python
autotrader.run_autotrade_and_parser()
```
- Парсит сигналы из Telegram каналов (через Telethon)
- Автоматически выполняет сделки для пользователей с VIP
- Применяет стратегии (Мартингейл, Процентная, Д'Аламбер)
- Проверяет статус открытых сделок

## ⚠️ Важные моменты

1. **Миграция с run_bot.py на main.py**
   - `run_bot.py` запускает только UI, без AI и AutoTrader
   - `main.py` - полнофункциональный монолитный сервис
   - Обновите команду запуска в Bothost!

2. **Переменные окружения**
   - Без `BOT_TOKEN` и `SUPABASE_URL` бот не запустится
   - `ANTHROPIC_API_KEY` опционален (без него не будет LLM-чата)
   - `TG_API_ID` опционален (без него не будет парсинга TG)

3. **Ресурсы**
   - Бот использует 3 асинхронных потока
   - Убедитесь, что у вас достаточно ресурсов на тарифе Pro
   - Рекомендуется: минимум 512MB RAM, 0.5 CPU

4. **Docker**
   - Dockerfile уже настроен с Chrome для Selenium
   - При сборке образа ошибки с apt-key исправлены
   - Образ собирается ~5-10 минут

## 📝 Логи и мониторинг

### Просмотр логов через бота
```
/logs
```

### Файл логов
```
/app/logs/bot.log
```

### Что должно быть в логах:

✅ **Успешный запуск:**
```
✅ Конфигурация валидна
✅ Database Manager инициализирован
✅ Все хэндлеры зарегистрированы
🔄 ЗАПУСК ПАРАЛЛЕЛЬНЫХ ПОТОКОВ
```

❌ **Ошибки:**
```
❌ Отсутствуют обязательные переменные окружения: BOT_TOKEN
❌ Ошибка инициализации Supabase: ...
```

## 🆘 Решение проблем

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте логи через `/logs`
2. Убедитесь, что все 3 потока запустились
3. Проверьте `BOT_TOKEN`

### Проблема: Нет сигналов
**Решение:**
1. Проверьте логи AI Core: "Итерация анализа #X"
2. Убедитесь, что yfinance установлен
3. Проверьте доступ к интернету

### Проблема: Не работает автоторговля
**Решение:**
1. Проверьте, что пользователь имеет VIP подписку
2. Убедитесь, что AutoTrader запущен
3. Проверьте наличие SSID пользователя

### Проблема: Не работает LLM-чат
**Решение:**
1. Проверьте `ANTHROPIC_API_KEY`
2. Убедитесь, что вы админ (ваш ID в `ADMIN_IDS`)
3. Проверьте баланс API Anthropic

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ Полнофункциональный монолитный сервис
- ✅ Telegram Bot UI для пользователей
- ✅ AI аналитика рынка в реальном времени
- ✅ Автоторговля с парсингом Telegram
- ✅ Админ-панель с LLM-чатом
- ✅ Все модули работают параллельно

**Поддержка:** @banana_pwr
