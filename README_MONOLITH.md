# 🚀 Монолитный Telegram-бот с AI-аналитикой

> Единый, мощный сервис для торговых сигналов с автоматизацией

## 📖 Описание

Это полнофункциональный монолитный сервис, который объединяет:

- 📱 **Telegram Bot UI** - интерфейс для пользователей
- 🛠️ **Админ-панель** - управление ботом и пользователями
- 🤖 **Автоторговля** - автоматическое выполнение сделок
- 🔍 **AI Аналитика** - анализ рынка через Claude AI
- 💬 **LLM-чат** - общение с AI для админов
- 📊 **Статистика** - детальная аналитика

## ✨ Особенности

✅ **Модульная архитектура** - 9 отдельных модулей для удобства поддержки  
✅ **Параллельные задачи** - UI, аналитика и автоторговля работают одновременно  
✅ **Безопасность** - шифрование SSID, проверка прав доступа  
✅ **Масштабируемость** - готов к работе с тысячами пользователей  
✅ **LLM-интеграция** - Claude AI для аналитики и чата  

## 📁 Структура проекта

```
/workspace/
├── main.py                  # 🎯 Точка входа (монолит)
├── requirements.txt         # 📦 Зависимости
│
├── config.py               # ⚙️ Конфигурация
├── db_manager.py           # 💾 База данных (Supabase)
├── crypto_utils.py         # 🔐 Шифрование
├── ai_core.py              # 🤖 AI аналитика
├── autotrader.py           # 📈 Автоторговля
├── admin_manager.py        # 🛠️ Админ-панель
├── ui_handlers.py          # 📱 UI обработчики
├── pocket_option_api.py    # 💼 Pocket Option API
│
├── .env.example            # 📝 Пример переменных окружения
├── DEPLOYMENT_GUIDE.md     # 📘 Руководство по деплою
└── README_MONOLITH.md      # 📖 Этот файл
```

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
# Перейдите в директорию проекта
cd /workspace

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

```bash
# Скопируйте пример
cp .env.example .env

# Отредактируйте .env и заполните своими значениями
nano .env
```

**Минимально необходимые переменные:**

```env
BOT_TOKEN=your_bot_token
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
ADMIN_IDS=your_telegram_id
ANTHROPIC_API_KEY=sk-ant-api03-...
```

### 3. Запуск

```bash
python main.py
```

## 🎮 Использование

### Команды для пользователей

```
/start       - Главное меню
/plans       - Тарифы и подписки
/bank        - Управление банком
/autotrade   - Автоторговля (VIP)
/short       - SHORT сигнал (1-5 мин)
/long        - LONG сигнал (1-4 часа)
/my_stats    - Моя статистика
/help        - Помощь
```

### Команды для админов

```
/manager     - Админ-панель
/logs        - Просмотр логов
/stats       - Статистика бота
```

### LLM-чат (для админов)

Просто напишите боту любое сообщение (не команду), и он ответит через Claude AI.

## 🏗️ Архитектура

### Три параллельных процесса

```python
await asyncio.gather(
    app.updater.start_polling(),        # 1. Telegram UI
    ai_core.run_analysis_cycle(),       # 2. AI Аналитика (каждые 5 мин)
    autotrader.run_autotrade_and_parser() # 3. Автоторговля (каждую минуту)
)
```

### Модули и их назначение

| Модуль | Назначение |
|--------|-----------|
| `main.py` | Точка входа, инициализация, запуск |
| `config.py` | Конфигурация из переменных окружения |
| `db_manager.py` | Работа с Supabase (CRUD, статистика) |
| `crypto_utils.py` | Шифрование/дешифрование SSID |
| `ai_core.py` | Аналитика рынка, генерация сигналов |
| `autotrader.py` | Автоторговля + парсинг Telegram |
| `admin_manager.py` | Админ-панель, LLM-чат |
| `ui_handlers.py` | Обработчики клиентских команд |
| `pocket_option_api.py` | API для Pocket Option |

## 📊 Функционал

### 🤖 AI Аналитика

- Технический анализ (RSI, MACD, Bollinger Bands, etc.)
- Генерация торговых сигналов
- Интеграция с yfinance для рыночных данных
- Анализ через Claude AI (опционально)

### 📈 Автоторговля

- Автоматическое выполнение сделок
- Парсинг сигналов из Telegram каналов
- Поддержка стратегий:
  - Мартингейл
  - Д'Аламбер
  - Процентная ставка
- Demo/Real режимы

### 🛠️ Админ-панель

- Статистика бота (пользователи, сигналы, подписки)
- Просмотр логов
- Управление пользователями
- LLM-чат с Claude AI

### 📱 Клиентский UI

- Тарифы и подписки (SHORT, LONG, VIP)
- Генерация сигналов
- Управление банком
- Статистика пользователя
- Настройки

## 🔐 Безопасность

✅ Шифрование SSID через `cryptography.Fernet`  
✅ Проверка прав админа по `ADMIN_IDS`  
✅ Безопасное хранение ключей в `.env`  
✅ Валидация входных данных  

## 📦 Зависимости

### Core
- `python-telegram-bot==20.7` - Telegram Bot API
- `supabase==2.3.1` - База данных
- `python-dotenv==1.0.0` - Переменные окружения

### AI & LLM
- `anthropic==0.34.0` - Claude AI API
- `pandas==2.1.4` - Анализ данных
- `ta==0.11.0` - Технический анализ
- `yfinance==0.2.32` - Рыночные данные

### Trading
- `telethon==1.33.1` - Telegram Client (парсинг)
- `cryptography==41.0.7` - Шифрование

Полный список: [requirements.txt](requirements.txt)

## 🌐 Деплой

### На BotHost

1. Создайте проект на [BotHost.ru](https://bothost.ru)
2. Загрузите все `.py` файлы
3. Установите переменные окружения
4. Запустите бота

### На VPS (Ubuntu/Debian)

```bash
# Установите Python 3.11+
sudo apt update
sudo apt install python3.11 python3.11-venv

# Клонируйте проект
git clone https://github.com/your-repo/bot.git
cd bot

# Создайте venv и установите зависимости
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Настройте .env
cp .env.example .env
nano .env

# Запустите через systemd
sudo nano /etc/systemd/system/trading-bot.service
```

**Содержимое `trading-bot.service`:**

```ini
[Unit]
Description=Trading Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/bot
Environment="PATH=/path/to/bot/venv/bin"
ExecStart=/path/to/bot/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# Запустите сервис
sudo systemctl daemon-reload
sudo systemctl enable trading-bot
sudo systemctl start trading-bot
sudo systemctl status trading-bot
```

## 📝 Логи

Логи сохраняются в `bot.log`:

```bash
# Просмотр в реальном времени
tail -f bot.log

# Последние 100 строк
tail -n 100 bot.log
```

Или через админ-команду:

```
/logs
```

## 🐛 Отладка

### Проверка окружения

```bash
python -c "from config import config; config.validate()"
```

### Проверка подключения к Supabase

```bash
python -c "from db_manager import DatabaseManager; print(DatabaseManager().get_global_stats())"
```

### Проверка Telegram Bot

```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe
```

## 📈 Мониторинг

### Метрики

- Количество пользователей
- Активных подписок
- Сгенерированных сигналов
- Пользователей с автоторговлей

### Алерты

Настройте уведомления при:
- Ошибках в логах
- Падении сервиса
- Критических ошибках БД

## 🤝 Вклад

Если вы хотите внести вклад:

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
2. Посмотрите логи: `tail -f bot.log`
3. Убедитесь, что все переменные окружения заданы
4. Напишите в поддержку: @banana_pwr

## 🎯 Roadmap

- [ ] Интеграция с Binance API
- [ ] Веб-панель для админов
- [ ] Мобильное приложение
- [ ] Поддержка мультиязычности
- [ ] A/B тестирование стратегий

---

**Сделано с ❤️ для трейдеров**

_Версия: 1.0 | Дата: 2025-12-09_
