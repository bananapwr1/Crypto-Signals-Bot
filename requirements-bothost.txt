# ======================================
# REQUIREMENTS для BotHost - ОБЛЕГЧЕННАЯ ВЕРСИЯ
# Только Telegram интерфейс, БЕЗ тяжелых зависимостей
# Версия: 1.0
# Дата: 2025-12-10
# ======================================

# ============ CORE DEPENDENCIES ============

# Python Telegram Bot API (v20+)
python-telegram-bot==20.7

# Supabase клиент для БД
supabase==2.3.1

# Переменные окружения
python-dotenv==1.0.0

# Асинхронный HTTP клиент
aiohttp==3.9.1

# Requests для синхронных HTTP запросов
requests==2.31.0


# ============ AI & LLM (опционально) ============

# Anthropic Claude API (для LLM-чата с админами)
anthropic==0.34.0


# ============ LIGHTWEIGHT TRADING ============

# TA-Lib альтернатива для технического анализа (легковесная)
# ВНИМАНИЕ: Работает в ограниченном режиме без pandas
ta==0.11.0

# WebSockets для real-time данных
websockets==12.0


# ============ CRYPTOGRAPHY & SECURITY ============

# Cryptography для шифрования SSID
cryptography==41.0.7

# PyJWT для работы с токенами
PyJWT==2.8.0


# ============ WEB FRAMEWORK (для webhook) ============

# Flask для webhook endpoints
Flask==3.0.0

# Flask-CORS для CORS
flask-cors==4.0.0


# ============ DATABASE ============

# psycopg2 для PostgreSQL (используется Supabase)
psycopg2-binary==2.9.9


# ============ UTILITIES ============

# python-dateutil для работы с датами
python-dateutil==2.8.2

# pytz для работы с часовыми поясами
pytz==2023.3.post1


# ============ LOGGING ============

# colorlog для цветного логирования
colorlog==6.8.0


# ============ ASYNC UTILITIES ============

# aiofiles для асинхронной работы с файлами
aiofiles==23.2.1

# ujson для быстрого JSON парсинга
ujson==5.9.0


# ======================================
# ИСКЛЮЧЕНЫ ДЛЯ BOTHOST:
# ======================================
# ❌ pandas (слишком тяжелый)
# ❌ numpy (слишком тяжелый)
# ❌ matplotlib (не нужен)
# ❌ selenium (требует Chrome)
# ❌ webdriver-manager (требует Chrome)
# ❌ yfinance (тяжелый, использует pandas)
# ❌ ccxt (очень тяжелый)
# ❌ openai (не используется)
# ❌ sqlalchemy (не используется)
# ❌ bcrypt (не используется)
# ❌ schedule (не используется)
# ❌ pytest (dev dependency)
# ❌ Pillow (не нужна графика)
