# 🔑 Руководство по настройке переменных окружения

## 📋 Финальный список из 9 переменных

Установите эти переменные в настройках вашего хостинга (BotHost/VPS) или в файле `.env`.

---

## 1️⃣ BOT_TOKEN

**Назначение:** Токен вашего Telegram бота

**Значение:**
```
8218904195:AAGinuQn0eGe8qYm-P5EOPwVq3awPyJ5fD8
```

**Как получить:**
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

---

## 2️⃣ NEXT_PUBLIC_SUPABASE_URL

**Назначение:** URL вашей базы данных Supabase

**Значение:**
```
https://qdilspmiaoxrnotarjnq.supabase.co
```

**Как получить:**
1. Зайдите на https://supabase.com
2. Создайте новый проект (или используйте существующий)
3. Перейдите в Settings → API
4. Скопируйте "Project URL"

---

## 3️⃣ NEXT_PUBLIC_SUPABASE_ANON_KEY

**Назначение:** Anon Key для доступа к Supabase

**Значение:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFkaWxzcG1pYW94cm5vdGFyam5xIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjA2MzkwMjUsImV4cCI6MjA3NjIxNTAyNX0.Ze0b8raeP1Pnhy3JYIjAL1fg3TVBLAHDM016r4ifruc
```

**Как получить:**
1. В том же разделе Settings → API
2. Скопируйте "anon public" ключ

---

## 4️⃣ ENCRYPTION_KEY

**Назначение:** Ключ шифрования для безопасного хранения SSID Pocket Option

**Значение:**
```
b0th0st_ru_s3cr3t_k3y_2024_encrypt_p0_data_s3cur3
```

**Рекомендации:**
- Используйте строку длиной 32+ символов
- Смешивайте буквы, цифры, символы
- Не используйте словарные слова

---

## 5️⃣ ADMIN_IDS

**Назначение:** ID администраторов бота (доступ к `/manager`, LLM-чату)

**Значение:**
```
7746862973,123456789
```

**Формат:** ID через запятую, без пробелов

**Как получить свой ID:**
1. Найдите @userinfobot в Telegram
2. Отправьте `/start`
3. Бот покажет ваш ID

---

## 6️⃣ ANTHROPIC_API_KEY

**Назначение:** Ключ для Claude AI (LLM-чат с админами, аналитика)

**Значение:**
```
sk-ant-api03-...
```

**Как получить:**
1. Зайдите на https://console.anthropic.com
2. Зарегистрируйтесь / войдите
3. Перейдите в API Keys
4. Создайте новый ключ
5. Пополните баланс ($5-10 для начала)

**Стоимость:**
- Claude 3.5 Sonnet: ~$3 за 1M токенов входящих, ~$15 за 1M исходящих
- Для бота с 10-20 админскими запросами в день: ~$1-2/месяц

---

## 7️⃣ TG_API_ID

**Назначение:** API ID для Telegram Client (Telethon) - парсинг каналов

**Значение:**
```
31068950
```

**Как получить:**
1. Зайдите на https://my.telegram.org/apps
2. Войдите с номером телефона
3. Создайте новое приложение (или используйте существующее)
4. Скопируйте "App api_id"

---

## 8️⃣ TG_API_HASH

**Назначение:** API Hash для Telegram Client

**Значение:**
```
4b96ce1f6dc3acf54118043d4e32fde2
```

**Как получить:**
- В том же разделе скопируйте "App api_hash"

---

## 9️⃣ TARGET_CHAT_ID

**Назначение:** ID каналов для парсинга торговых сигналов

**Значение:**
```
-1001829321008,-1001597052825
```

**Формат:** ID через запятую, без пробелов

**Как получить ID канала:**

### Метод 1: Через веб-версию
1. Откройте канал в веб-версии Telegram (web.telegram.org)
2. Посмотрите в URL: `https://web.telegram.org/k/#-1001234567890`
3. ID канала: `-1001234567890`

### Метод 2: Через бота
1. Создайте бота @getmyid_bot
2. Добавьте его в канал
3. Бот покажет ID канала

### Метод 3: Через Python
```python
from telethon import TelegramClient

api_id = 31068950
api_hash = '4b96ce1f6dc3acf54118043d4e32fde2'

client = TelegramClient('session', api_id, api_hash)
client.start()

async def get_chat_id():
    async for dialog in client.iter_dialogs():
        print(dialog.name, dialog.id)

client.loop.run_until_complete(get_chat_id())
```

---

## 📦 Установка на BotHost

### Вариант 1: Через веб-интерфейс

1. Зайдите в панель управления BotHost
2. Выберите ваш проект
3. Перейдите в раздел "Переменные окружения" или "Environment Variables"
4. Добавьте каждую переменную:
   - **Имя:** `BOT_TOKEN`
   - **Значение:** `8218904195:AAGinuQn0eGe8qYm-P5EOPwVq3awPyJ5fD8`
5. Сохраните и перезапустите бот

### Вариант 2: Через .env файл

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Отредактируйте `.env`:
   ```bash
   nano .env
   ```

3. Загрузите `.env` на сервер (если используете VPS)

⚠️ **ВАЖНО:** Не коммитьте `.env` в Git! Убедитесь, что `.env` есть в `.gitignore`.

---

## ✅ Проверка настройки

После установки всех переменных запустите бота локально:

```bash
python main.py
```

**Ожидаемый вывод:**

```
==============================================================
🚀 ЗАПУСК МОНОЛИТНОГО СЕРВИСА
==============================================================
✅ Конфигурация валидна
📝 BOT_TOKEN: 8218904195...
📝 SUPABASE_URL: https://qdilspmiaoxrnotarjnq...
📝 ADMIN_IDS: [7746862973, 123456789]
✅ ANTHROPIC_API_KEY найден: sk-ant-api...
==============================================================
```

Если видите ошибки типа:
- `❌ BOT_TOKEN не найден` → проверьте название переменной
- `❌ Ошибка конфигурации: ...` → проверьте формат значений

---

## 🔐 Безопасность

### ✅ Хорошие практики:

1. **Не делитесь токенами** - никогда не публикуйте `.env` файл
2. **Регулярно обновляйте** - меняйте токены раз в 3-6 месяцев
3. **Используйте права доступа** - на VPS установите `chmod 600 .env`
4. **Мониторьте использование** - проверяйте расход API ключей

### ❌ Плохие практики:

- ❌ Хранить токены в коде
- ❌ Коммитить `.env` в Git
- ❌ Использовать слабые ключи шифрования
- ❌ Давать токены третьим лицам

---

## 🆘 Решение проблем

### Бот не запускается

**Проблема:** `ValueError: BOT_TOKEN не задан`

**Решение:**
1. Убедитесь, что переменная называется **точно** `BOT_TOKEN` (регистр важен)
2. Проверьте, что нет лишних пробелов в начале/конце
3. Перезапустите бот

---

### LLM-чат не работает

**Проблема:** `⚠️ LLM-чат недоступен`

**Решение:**
1. Проверьте, что `ANTHROPIC_API_KEY` задан корректно
2. Убедитесь, что на счету Anthropic есть средства
3. Проверьте, что ваш ID в списке `ADMIN_IDS`

---

### Парсинг Telegram не работает

**Проблема:** `⚠️ Telegram Client недоступен`

**Решение:**
1. Проверьте `TG_API_ID` и `TG_API_HASH`
2. Убедитесь, что ID каналов в `TARGET_CHAT_ID` правильные
3. Проверьте, что бот имеет доступ к этим каналам

---

### База данных недоступна

**Проблема:** `❌ Supabase клиент не инициализирован`

**Решение:**
1. Проверьте `NEXT_PUBLIC_SUPABASE_URL`
2. Проверьте `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. Убедитесь, что проект Supabase активен

---

## 📞 Поддержка

Если проблемы остались:

1. Проверьте логи: `tail -f bot.log`
2. Запустите команду `/logs` в боте (для админов)
3. Напишите в поддержку: @banana_pwr

---

**Версия:** 1.0  
**Дата:** 2025-12-09  
**Готово к деплою!** 🚀
