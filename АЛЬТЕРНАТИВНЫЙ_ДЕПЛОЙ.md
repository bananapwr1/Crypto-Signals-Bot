# 🚀 Альтернативные способы деплоя бота

Если Bothost не работает или вы хотите попробовать другую платформу.

---

## 1. Railway.app (Рекомендуется) ⭐

**Преимущества:**
- ✅ Простой и быстрый деплой
- ✅ Бесплатный тариф $5 кредитов/месяц
- ✅ Автоматический деплой из GitHub
- ✅ Поддержка Docker
- ✅ Логи в реальном времени

### Инструкция

#### Шаг 1: Регистрация
1. Перейдите на https://railway.app
2. Нажмите "Login" → "GitHub"
3. Авторизуйтесь через GitHub

#### Шаг 2: Создание проекта
```bash
# Установка Railway CLI (опционально)
npm i -g @railway/cli

# Или через веб-интерфейс:
1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий: Crypto-Signals-Bot
4. Railway автоматически определит Dockerfile
```

#### Шаг 3: Настройка переменных окружения
В панели Railway:
1. Откройте проект
2. Перейдите в "Variables"
3. Добавьте переменные:

```env
BOT_TOKEN=ваш_токен_от_BotFather
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ_supabase
ADMIN_IDS=7746862973
ANTHROPIC_API_KEY=ваш_ключ_anthropic (опционально)
ENCRYPTION_KEY=любая_строка_32_символа
```

#### Шаг 4: Деплой
Railway автоматически задеплоит ваш бот после добавления переменных!

**Готово!** ✅ Бот будет работать 24/7

---

## 2. Render.com

**Преимущества:**
- ✅ Бесплатный тариф (ограничения: засыпает после 15 мин простоя)
- ✅ GitHub интеграция
- ✅ Простая настройка
- ✅ Web Dashboard

### Инструкция

#### Шаг 1: Регистрация
1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub

#### Шаг 2: Создание Web Service
```
1. Dashboard → "New +" → "Web Service"
2. Подключите GitHub репозиторий
3. Настройки:
   - Name: crypto-signals-bot
   - Environment: Docker
   - Region: Frankfurt (ближе к России)
   - Instance Type: Free
```

#### Шаг 3: Переменные окружения
Добавьте в разделе "Environment":

```env
BOT_TOKEN=ваш_токен
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ
ADMIN_IDS=7746862973
```

#### Шаг 4: Деплой
Нажмите "Create Web Service" - деплой начнется автоматически!

**⚠️ Примечание:** На бесплатном тарифе сервис засыпает после 15 минут неактивности. Используйте платный тариф ($7/мес) для 24/7 работы.

---

## 3. Fly.io

**Преимущества:**
- ✅ Бесплатный тариф (3 микро VM)
- ✅ Глобальная сеть
- ✅ Docker native
- ✅ Низкая задержка

### Инструкция

#### Шаг 1: Установка Fly CLI
```bash
# Linux/Mac
curl -L https://fly.io/install.sh | sh

# Windows (PowerShell)
iwr https://fly.io/install.ps1 -useb | iex
```

#### Шаг 2: Авторизация
```bash
fly auth login
```

#### Шаг 3: Создание приложения
```bash
cd /path/to/Crypto-Signals-Bot
fly launch
```

Ответьте на вопросы:
- App name: crypto-signals-bot (или оставьте автоматическое)
- Region: ams (Amsterdam - ближе к России)
- Database: No
- Deploy now: No (сначала настроим переменные)

#### Шаг 4: Настройка переменных
```bash
fly secrets set BOT_TOKEN="ваш_токен"
fly secrets set NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
fly secrets set NEXT_PUBLIC_SUPABASE_ANON_KEY="ваш_ключ"
fly secrets set ADMIN_IDS="7746862973"
fly secrets set ENCRYPTION_KEY="random-32-char-string"
```

#### Шаг 5: Деплой
```bash
fly deploy
```

#### Шаг 6: Проверка логов
```bash
fly logs
```

---

## 4. VPS (DigitalOcean / Hetzner)

**Преимущества:**
- ✅ Полный контроль
- ✅ Стабильная работа
- ✅ Низкая цена ($5-10/мес)
- ✅ Без ограничений

**Недостатки:**
- ❌ Требует настройки сервера
- ❌ Нужны базовые знания Linux

### Инструкция для Ubuntu 22.04

#### Шаг 1: Создание VPS
Создайте VPS на:
- DigitalOcean (от $6/мес)
- Hetzner (от €4/мес)
- VDSina.ru (от 200₽/мес)

Выберите:
- OS: Ubuntu 22.04
- RAM: минимум 1GB
- Region: ближайший к вам

#### Шаг 2: Подключение по SSH
```bash
ssh root@your_vps_ip
```

#### Шаг 3: Установка Docker
```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Проверка
docker --version
```

#### Шаг 4: Клонирование репозитория
```bash
cd /opt
git clone https://github.com/bananapwr1/Crypto-Signals-Bot.git
cd Crypto-Signals-Bot
```

#### Шаг 5: Создание .env файла
```bash
nano .env
```

Вставьте:
```env
BOT_TOKEN=ваш_токен
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ
ADMIN_IDS=7746862973
ANTHROPIC_API_KEY=ваш_ключ (опционально)
TG_API_ID=12345678 (опционально)
TG_API_HASH=your_hash (опционально)
ENCRYPTION_KEY=random-32-char-string
FLASK_SECRET_KEY=another-random-string
```

Сохраните: `Ctrl+X` → `Y` → `Enter`

#### Шаг 6: Сборка и запуск
```bash
# Сборка образа
docker build -t crypto-bot .

# Запуск контейнера
docker run -d \
  --name crypto-signals-bot \
  --restart unless-stopped \
  --env-file .env \
  crypto-bot

# Проверка логов
docker logs -f crypto-signals-bot
```

#### Шаг 7: Автозапуск при перезагрузке
```bash
# Создание systemd service
nano /etc/systemd/system/crypto-bot.service
```

Вставьте:
```ini
[Unit]
Description=Crypto Signals Bot
After=docker.service
Requires=docker.service

[Service]
Type=simple
Restart=always
WorkingDirectory=/opt/Crypto-Signals-Bot
ExecStart=/usr/bin/docker start -a crypto-signals-bot
ExecStop=/usr/bin/docker stop crypto-signals-bot

[Install]
WantedBy=multi-user.target
```

```bash
# Активация
systemctl enable crypto-bot
systemctl start crypto-bot
systemctl status crypto-bot
```

#### Управление ботом
```bash
# Просмотр логов
docker logs -f crypto-signals-bot

# Остановка
docker stop crypto-signals-bot

# Запуск
docker start crypto-signals-bot

# Перезапуск
docker restart crypto-signals-bot

# Обновление из GitHub
cd /opt/Crypto-Signals-Bot
git pull
docker build -t crypto-bot .
docker stop crypto-signals-bot
docker rm crypto-signals-bot
docker run -d --name crypto-signals-bot --restart unless-stopped --env-file .env crypto-bot
```

---

## 5. Heroku (Платный с 2022 года)

**⚠️ Внимание:** Heroku отменил бесплатный тариф. Минимум $5/мес.

### Инструкция
```bash
# Установка Heroku CLI
curl https://cli-assets.heroku.com/install.sh | sh

# Авторизация
heroku login

# Создание приложения
heroku create crypto-signals-bot

# Настройка переменных
heroku config:set BOT_TOKEN="ваш_токен"
heroku config:set NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
heroku config:set NEXT_PUBLIC_SUPABASE_ANON_KEY="ваш_ключ"
heroku config:set ADMIN_IDS="7746862973"

# Деплой
git push heroku main

# Логи
heroku logs --tail
```

---

## 📊 Сравнение платформ

| Платформа | Цена/мес | Сложность | 24/7 | Рекомендация |
|-----------|----------|-----------|------|--------------|
| Railway   | $5 кредит | ⭐ Легко | ✅ Да | ⭐⭐⭐⭐⭐ |
| Render    | Free* | ⭐ Легко | ❌ Нет* | ⭐⭐⭐ |
| Fly.io    | Free | ⭐⭐ Средне | ✅ Да | ⭐⭐⭐⭐ |
| VPS       | $5-10 | ⭐⭐⭐ Сложно | ✅ Да | ⭐⭐⭐⭐ |
| Heroku    | $5+ | ⭐ Легко | ✅ Да | ⭐⭐⭐ |

*Free тариф Render засыпает после 15 мин простоя

---

## 🎯 Наша рекомендация

### Для быстрого тестирования:
**Railway** - самый простой и быстрый способ

### Для продакшена:
**VPS (Hetzner/DigitalOcean)** - полный контроль, низкая цена, стабильность

### Для минимального бюджета:
**Fly.io** - бесплатный тариф с хорошими ограничениями

---

## ❓ Нужна помощь?

Выберите платформу и я помогу с детальной настройкой!

**Контакт:** @banana_pwr (Telegram)

---

**Дата:** 10.12.2025  
**Версия:** 1.0
