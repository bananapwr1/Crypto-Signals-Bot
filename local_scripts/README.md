# 🤖 Автоматизация получения SSID для Pocket Option

## 📋 Описание

Этот скрипт **работает локально на вашем компьютере** и автоматически получает SSID из Pocket Option, отправляя его напрямую вашему Telegram боту.

### ✅ Преимущества:
- 🔒 **Безопасность**: Логин и пароль хранятся только на вашем компьютере
- ⚡ **Автоматизация**: Не нужно вручную копировать SSID через DevTools
- 🔄 **Обновление**: Можно настроить автоматическое обновление SSID
- 💾 **Резервная копия**: SSID сохраняется локально в файл

### ⚠️ Важно понимать:
- Скрипт **НЕ работает на сервере бота**
- Ваши данные **НЕ передаются** на сторонние серверы
- Требуется **Chrome браузер** и **ChromeDriver**

---

## 🚀 Установка

### Шаг 1: Установите Python 3.8+

**Windows:**
1. Скачайте с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"

**macOS:**
```bash
brew install python3
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

### Шаг 2: Установите Chrome браузер

Скачайте и установите [Google Chrome](https://www.google.com/chrome/)

### Шаг 3: Установите ChromeDriver

**Автоматическая установка (рекомендуется):**
```bash
pip install webdriver-manager
```

**Ручная установка:**
1. Узнайте версию Chrome: `chrome://version`
2. Скачайте соответствующий ChromeDriver: [chromedriver.chromium.org](https://chromedriver.chromium.org/)
3. Распакуйте и добавьте в PATH

### Шаг 4: Установите зависимости Python

```bash
cd local_scripts
pip install -r requirements.txt
```

---

## ⚙️ Настройка

### 1. Создайте файл .env

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

### 2. Заполните данные в .env

Откройте файл `.env` в текстовом редакторе и заполните:

```env
# Email от Pocket Option
POCKET_EMAIL=your_email@example.com

# Пароль от Pocket Option
POCKET_PASSWORD=your_password

# Ваш Telegram User ID (получите из бота командой /start)
TELEGRAM_USER_ID=123456789

# Токен вашего бота (получите у администратора)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 3. Узнайте ваш Telegram User ID

1. Откройте бота
2. Отправьте команду `/start`
3. Бот покажет ваш User ID

---

## 🎯 Использование

### Разовое получение SSID

```bash
python ssid_auto_extractor.py
```

### Что произойдет:

1. 🌐 Откроется Chrome браузер
2. 🔐 Скрипт автоматически войдет в Pocket Option
3. 🔍 Извлечет SSID из cookies/localStorage
4. 💾 Сохранит SSID в файл `ssid_backup.txt`
5. 📤 Отправит SSID вашему боту в Telegram
6. ✅ Бот автоматически обновит SSID для автотрейдинга

---

## 🔄 Автоматическое обновление (опционально)

### Создайте планировщик для периодического обновления

**Windows (Task Scheduler):**

1. Откройте "Планировщик заданий"
2. Создайте новую задачу
3. Триггер: каждые 12 часов
4. Действие: `python C:\path\to\ssid_auto_extractor.py`

**macOS/Linux (cron):**

```bash
# Редактировать crontab
crontab -e

# Добавить строку (запуск каждые 12 часов)
0 */12 * * * cd /path/to/local_scripts && python3 ssid_auto_extractor.py
```

---

## 🛠️ Решение проблем

### Ошибка: ChromeDriver не найден

**Решение:**
```bash
pip install webdriver-manager
```

Или скачайте ChromeDriver вручную и добавьте в PATH.

### Ошибка: Не удалось войти в аккаунт

**Возможные причины:**
- ❌ Неверный email или пароль
- ❌ Pocket Option изменил интерфейс входа
- ❌ Включена двухфакторная аутентификация (2FA)

**Решение:**
1. Проверьте credentials в `.env`
2. Попробуйте войти вручную в браузере
3. Отключите 2FA в настройках Pocket Option (если включена)

### Ошибка: SSID не найден

**Решение:**
1. Убедитесь, что вход прошел успешно
2. Проверьте cookies в браузере после входа
3. Попробуйте подождать дольше после входа (увеличьте `time.sleep()`)

### Ошибка: Не отправляется боту

**Проверьте:**
- ✅ Правильность `BOT_TOKEN` в `.env`
- ✅ Правильность `TELEGRAM_USER_ID`
- ✅ Интернет-соединение

---

## 🔒 Безопасность

### ✅ Что безопасно:
- Скрипт работает локально на вашем ПК
- Credentials хранятся в `.env` только у вас
- SSID отправляется напрямую вашему боту

### ⚠️ Важные правила:
1. **Никогда** не загружайте `.env` на GitHub
2. **Не делитесь** файлом `.env` с другими
3. **Храните** скрипт в безопасном месте
4. **Используйте** только на своем компьютере

---

## 📝 FAQ

**Q: Нужно ли держать скрипт запущенным постоянно?**  
A: Нет, скрипт запускается разово для получения SSID. Можно настроить автозапуск по расписанию.

**Q: Можно ли использовать на сервере?**  
A: Технически да, но **НЕ РЕКОМЕНДУЕТСЯ** по соображениям безопасности. Используйте только локально.

**Q: Как часто нужно обновлять SSID?**  
A: Обычно SSID действует несколько дней/недель. Обновляйте при ошибках автотрейдинга.

**Q: Безопасно ли хранить пароль в .env?**  
A: На вашем локальном ПК - относительно безопасно. Убедитесь, что файл защищен и не синхронизируется в облако.

**Q: Что делать, если Pocket Option обновил интерфейс?**  
A: Скрипт может перестать работать. Сообщите администратору бота для обновления скрипта.

---

## 📞 Поддержка

Если возникли проблемы:

1. 📖 Прочитайте раздел "Решение проблем"
2. 💬 Напишите в поддержку бота: @banana_pwr
3. 🐛 Опишите ошибку подробно с текстом из консоли

---

## 📜 Changelog

### v1.0.0 (2025-01-16)
- 🎉 Первый релиз
- ✨ Автоматическое получение SSID
- 📤 Отправка боту через Telegram API
- 💾 Локальное сохранение резервной копии

---

**Сделано с ❤️ для пользователей Crypto Signals Bot**
