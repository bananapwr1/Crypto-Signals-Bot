# Изменения: Разделение логики парсера

**Дата:** 2025-12-10  
**Версия:** 2.0

## Обзор изменений

Вся логика парсинга Telegram сигналов была удалена из основного бота. Парсер теперь запускается на отдельном хостинге и отправляет сигналы в БД.

---

## Новая архитектура

### Внешний парсер (на отдельном хостинге)
- Парсит сигналы из Telegram каналов
- Сохраняет сигналы в БД Supabase с полем `source: 'parser'` или `'external'`
- Работает независимо от основного бота

### Основной бот

#### AI Core (`ai_core.py`)
- Проводит **собственный анализ рынка**
- Генерирует сигналы на основе технического анализа
- **Использует внешние сигналы** из парсера для:
  - Обучения модели
  - Улучшения анализа
  - Сравнения с собственными сигналами
- Сохраняет собственные сигналы с полем `source: 'ai_core'`

#### AutoTrader (`autotrader.py`)
- Выполняет сделки для пользователей с автоторговлей
- Работает **только с сигналами от AI Core**
- Проверяет статус открытых сделок
- Управляет стратегиями (Мартингейл, Процентная ставка, Д'Аламбер)

---

## Изменённые файлы

### 1. `autotrader.py`
**Удалено:**
- Импорт `telethon` библиотеки
- Класс `TelegramClient`
- Методы парсинга сообщений Telegram:
  - `init_telegram_client()`
  - `parse_signal_message()`
  - `extract_signal_from_text()`
- Переменные окружения для Telegram API:
  - `TG_API_ID`
  - `TG_API_HASH`
  - `TARGET_CHAT_ID`

**Обновлено:**
- Метод `run_autotrade_cycle()` - теперь без парсинга
- Комментарии и документация

### 2. `ai_core.py`
**Добавлено:**
- Метод `get_external_signals()` - получение сигналов от парсера
- Метод `analyze_external_signals()` - анализ внешних сигналов для обучения
- Интеграция внешних сигналов в цикл анализа
- Пометка собственных сигналов как `source: 'ai_core'`

**Обновлено:**
- Метод `run_analysis_cycle()` - добавлена работа с внешними сигналами
- Комментарии и документация

### 3. `db_manager.py`
**Добавлено:**
- Метод `get_external_signals()` - получение сигналов из БД с источником parser/external
- Метод `mark_signal_as_processed()` - отметка обработанных сигналов

### 4. `main.py`
**Обновлено:**
- Комментарии о работе без парсинга
- Название метода: `run_autotrade_and_parser()` → `run_autotrade_cycle()`
- Документация

### 5. `requirements.txt`
**Удалено:**
- `telethon==1.33.1` - библиотека для Telegram Client

---

## Структура данных в БД

### Таблица `signals`

Теперь сигналы различаются по полю `source`:

```json
{
  "id": 1,
  "symbol": "BTC-USD",
  "signal_type": "CALL",
  "confidence": 75.0,
  "entry_price": 45000.0,
  "timeframe": "5m",
  "source": "ai_core",  // или "parser", "external"
  "created_at": "2025-12-10T12:00:00Z",
  "processed": false,
  "processed_at": null
}
```

### Типы источников:
- `ai_core` - сигналы от AI Core (технический анализ)
- `parser` - сигналы от внешнего парсера Telegram
- `external` - другие внешние источники

---

## Workflow

### 1. Внешний парсер (отдельный хостинг)
```
Telegram канал → Парсер → Supabase БД (source: 'parser')
```

### 2. AI Core (основной бот)
```
yfinance (рыночные данные)
    ↓
Технический анализ (RSI, MACD, Bollinger, etc.)
    ↓
Генерация сигнала
    ↓
Supabase БД (source: 'ai_core')
    ↓
← Получение внешних сигналов для обучения
```

### 3. AutoTrader (основной бот)
```
Получение пользователей с auto_trading_enabled = true
    ↓
Получение сигналов AI Core из БД
    ↓
Расчёт ставки по стратегии
    ↓
Выполнение сделки через Pocket Option API
```

---

## Преимущества новой архитектуры

### ✅ Разделение ответственности
- Парсер работает независимо
- AI Core фокусируется на анализе
- AutoTrader только выполняет сделки

### ✅ Масштабируемость
- Парсер может работать на отдельном сервере
- Можно запустить несколько инстансов парсера
- Основной бот не зависит от парсера

### ✅ Надёжность
- Сбой парсера не влияет на бота
- Бот продолжает работать с собственным анализом
- Независимое развёртывание компонентов

### ✅ Гибкость
- Легко добавить новые источники сигналов
- Внешние сигналы используются для обучения
- AI Core может принимать решения на основе множества источников

---

## Переменные окружения

### Больше НЕ нужны:
- ❌ `TG_API_ID`
- ❌ `TG_API_HASH`
- ❌ `TARGET_CHAT_ID`

### Всё ещё требуются:
- ✅ `TELEGRAM_TOKEN` (для Telegram Bot UI)
- ✅ `NEXT_PUBLIC_SUPABASE_URL`
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- ✅ `ANTHROPIC_API_KEY` (опционально, для LLM)
- ✅ `POCKET_OPTION_SSID` (для торговли)

---

## Развёртывание

### Основной бот
```bash
# Устанавливаем зависимости (telethon больше не нужен)
pip install -r requirements.txt

# Запускаем бота
python main.py
```

### Внешний парсер (отдельно)
```bash
# Создайте отдельный проект с telethon
pip install telethon supabase python-dotenv

# Парсер должен:
# 1. Подключаться к Telegram каналам
# 2. Парсить сигналы
# 3. Сохранять в Supabase с source='parser'
```

---

## Миграция

### Если у вас уже был запущен старый бот:

1. **Остановите старый бот**
```bash
# Остановите процесс
pkill -f main.py
```

2. **Обновите код**
```bash
git pull origin main
```

3. **Обновите зависимости**
```bash
pip install -r requirements.txt
```

4. **Удалите переменные окружения парсера** (необязательно)
```bash
# В .env удалите:
# TG_API_ID
# TG_API_HASH
# TARGET_CHAT_ID
```

5. **Запустите новый бот**
```bash
python main.py
```

---

## Пример внешнего парсера

Создайте отдельный файл `telegram_parser.py`:

```python
import os
from telethon import TelegramClient, events
from supabase import create_client
from datetime import datetime, timezone

# Инициализация
tg_client = TelegramClient('parser_session', 
                           int(os.getenv('TG_API_ID')), 
                           os.getenv('TG_API_HASH'))

supabase = create_client(
    os.getenv('NEXT_PUBLIC_SUPABASE_URL'),
    os.getenv('NEXT_PUBLIC_SUPABASE_ANON_KEY')
)

TARGET_CHAT_IDS = [-1001234567890]  # Ваши каналы

@tg_client.on(events.NewMessage(chats=TARGET_CHAT_IDS))
async def handle_message(event):
    text = event.message.text
    
    # Парсинг сигнала
    signal = parse_signal(text)  # Ваша логика
    
    if signal:
        # Сохранение в БД
        supabase.table('signals').insert({
            'symbol': signal['symbol'],
            'signal_type': signal['type'],
            'source': 'parser',  # Важно!
            'confidence': 70.0,
            'timeframe': signal['timeframe'],
            'created_at': datetime.now(timezone.utc).isoformat()
        }).execute()

# Запуск
tg_client.start()
tg_client.run_until_disconnected()
```

---

## Тестирование

### 1. Проверка AI Core
```python
# Запустите бота и проверьте логи
# Должны видеть:
# ✅ AI Core инициализирован
# 📊 Режим: собственный анализ + обучение на внешних сигналах
```

### 2. Проверка AutoTrader
```python
# В логах должно быть:
# ✅ AutoTrader инициализирован
# 📊 Режим работы: получение сигналов из БД
# 🤖 Сделки выполняются на основе анализа AI Core
```

### 3. Проверка БД
```sql
-- Проверьте таблицу signals
SELECT source, COUNT(*) 
FROM signals 
GROUP BY source;

-- Должны быть источники: 'ai_core', 'parser', 'external'
```

---

## FAQ

**Q: Как теперь получить сигналы из Telegram?**  
A: Запустите отдельный парсер на другом хостинге. Он будет сохранять сигналы в БД.

**Q: Бот теперь не торгует по внешним сигналам?**  
A: Правильно. Бот использует собственный анализ AI Core. Внешние сигналы нужны для обучения и улучшения модели.

**Q: Где логика парсинга?**  
A: Парсер вынесен в отдельный проект. См. пример выше.

**Q: Нужно ли обновлять БД?**  
A: Нет, структура БД совместима. Просто добавьте поле `source` в таблицу `signals` (если его нет).

---

## Поддержка

Если возникли вопросы или проблемы:
1. Проверьте логи: `tail -f bot.log`
2. Убедитесь, что все переменные окружения заданы
3. Проверьте подключение к Supabase

---

**Конец документа**
