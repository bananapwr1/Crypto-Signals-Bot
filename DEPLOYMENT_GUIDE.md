# 🚀 Руководство по деплою монолитного сервиса

## 📋 Содержание

1. [Структура проекта](#структура-проекта)
2. [Переменные окружения](#переменные-окружения)
3. [Установка зависимостей](#установка-зависимостей)
4. [Запуск сервиса](#запуск-сервиса)
5. [Настройка на BotHost](#настройка-на-bothost)

---

## 📁 Структура проекта

```
/workspace/
├── main.py                  # Точка входа (монолитный сервис)
├── requirements.txt         # Зависимости Python
│
├── config.py               # Конфигурация (переменные окружения)
├── db_manager.py           # Менеджер базы данных (Supabase)
├── crypto_utils.py         # Шифрование SSID
├── ai_core.py              # AI аналитика рынка
├── autotrader.py           # Автоторговля + парсинг Telegram
├── admin_manager.py        # Админ-панель + LLM-чат
├── ui_handlers.py          # UI обработчики для клиентов
├── pocket_option_api.py    # API для Pocket Option
│
└── DEPLOYMENT_GUIDE.md     # Это руководство
```

---

## 🔑 Переменные окружения

### Обязательные переменные

Создайте файл `.env` или установите в BotHost:

```bash
# ========================================
# TELEGRAM BOT
# ========================================
BOT_TOKEN=your_bot_token_here

# ========================================
# SUPABASE DATABASE
# ========================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ========================================
# ADMIN
# ========================================
ADMIN_IDS=7746862973,123456789  # Список ID админов через запятую

# ========================================
# LLM (для админ-чата и аналитики)
# ========================================
ANTHROPIC_API_KEY=sk-ant-api03-...  # Claude API ключ

# ========================================
# TELEGRAM CLIENT (для парсинга каналов)
# ========================================
TG_API_ID=your_api_id
TG_API_HASH=your_api_hash
TARGET_CHAT_ID=-1001234567890,-1009876543210  # Список каналов

# ========================================
# ENCRYPTION (для SSID)
# ========================================
ENCRYPTION_KEY=your-32-character-encryption-key-here

# ========================================
# YOOKASSA (опционально, для платежей)
# ========================================
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
```

---

## 📦 Установка зависимостей

### Локально (для тестирования)

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### На сервере (BotHost / VPS)

```bash
pip install -r requirements.txt
```

---

## 🚀 Запуск сервиса

### Локальный запуск

```bash
python main.py
```

### Запуск на BotHost

1. Загрузите все файлы на BotHost
2. Установите переменные окружения (см. выше)
3. BotHost автоматически запустит `main.py`

### Проверка работы

После запуска вы должны увидеть:

```
==============================================================
🚀 ЗАПУСК МОНОЛИТНОГО СЕРВИСА
==============================================================
✅ Конфигурация валидна
📝 BOT_TOKEN: 7861384519...
📝 SUPABASE_URL: https://mgxbfxipxwxvlbvtfbcg...
📝 ADMIN_IDS: [7746862973]
✅ ANTHROPIC_API_KEY найден: sk-ant-api...
==============================================================
🔍 Инициализация модулей...
==============================================================
✅ DatabaseManager инициализирован
✅ PocketOptionAPI инициализирован
✅ AICore инициализирован
✅ AutoTrader инициализирован
✅ AdminManager инициализирован
✅ UIHandlers инициализирован
==============================================================
✅ Инициализация модулей завершена
==============================================================
📱 Создание Telegram Application...
✅ Команды бота настроены
✅ Telegram Application готов
==============================================================
🎯 СИСТЕМА ЗАПУЩЕНА!
==============================================================
📱 Telegram Bot: ACTIVE
🔍 Аналитика: STARTING
🤖 Автоторговля: STARTING
==============================================================
```

---

## ⚙️ Настройка на BotHost

### 1. Создание проекта

1. Зайдите на [BotHost.ru](https://bothost.ru)
2. Создайте новый проект Python
3. Выберите Python 3.11+

### 2. Загрузка файлов

Загрузите все файлы:

- `main.py`
- `requirements.txt`
- `config.py`
- `db_manager.py`
- `crypto_utils.py`
- `ai_core.py`
- `autotrader.py`
- `admin_manager.py`
- `ui_handlers.py`
- `pocket_option_api.py`

### 3. Установка переменных окружения

В панели BotHost перейдите в "Переменные окружения" и добавьте все необходимые переменные (см. раздел выше).

### 4. Запуск

Нажмите "Запустить" в панели BotHost.

---

## 🔧 Архитектура и особенности

### Параллельные задачи

Сервис запускает 3 параллельные задачи через `asyncio.gather`:

1. **Telegram Polling** - обработка команд от пользователей
2. **AI Analysis Cycle** - непрерывный анализ рынка (каждые 5 минут)
3. **AutoTrade Cycle** - автоторговля + парсинг Telegram каналов (каждую минуту)

### Модульная архитектура

Каждый модуль отвечает за свою область:

- **config.py** - конфигурация
- **db_manager.py** - работа с Supabase
- **crypto_utils.py** - шифрование SSID
- **ai_core.py** - аналитика рынка через LLM
- **autotrader.py** - автоторговля + парсинг Telegram
- **admin_manager.py** - админ-панель + LLM-чат
- **ui_handlers.py** - клиентский UI
- **pocket_option_api.py** - интеграция с Pocket Option

### Graceful Shutdown

При остановке (Ctrl+C) все задачи корректно завершаются:

```python
try:
    await asyncio.gather(...)
except KeyboardInterrupt:
    logger.info("⚠️ Получен сигнал остановки (Ctrl+C)")
finally:
    await app.stop()
    await app.shutdown()
```

---

## 🐛 Отладка

### Просмотр логов

Логи сохраняются в файл `bot.log`:

```bash
tail -f bot.log
```

Или через админ-панель бота:

```
/logs
```

### Проверка подключения к Supabase

```python
from db_manager import DatabaseManager

db = DatabaseManager()
stats = db.get_global_stats()
print(stats)
```

### Проверка Telegram Bot

```bash
curl https://api.telegram.org/bot<BOT_TOKEN>/getMe
```

---

## 📊 Команды бота

### Клиентские команды

- `/start` - Главное меню
- `/plans` - Тарифы и подписки
- `/bank` - Управление банком
- `/autotrade` - Автоторговля (VIP)
- `/settings` - Настройки
- `/short` - SHORT сигнал (1-5 мин)
- `/long` - LONG сигнал (1-4 часа)
- `/my_longs` - Мои LONG позиции
- `/my_stats` - Моя статистика
- `/help` - Помощь

### Админские команды

- `/manager` - Админ-панель
- `/logs` - Просмотр логов
- `/stats` - Статистика бота

### LLM-чат (для админов)

Просто напишите боту любое сообщение (не команду), и он ответит через Claude AI.

---

## 🔐 Безопасность

1. **Шифрование SSID**: Все SSID шифруются перед сохранением в БД
2. **Проверка прав**: Админские команды доступны только пользователям из `ADMIN_IDS`
3. **Безопасные переменные**: Все ключи хранятся в переменных окружения, не в коде

---

## 📈 Мониторинг

### Проверка состояния

Используйте админ-команды:

```
/stats - глобальная статистика
/logs - последние логи
```

### Метрики

- Количество пользователей
- Активных подписок
- Сгенерированных сигналов
- Пользователей с автоторговлей

---

## ❓ FAQ

### Q: Бот не отвечает на команды

**A:** Проверьте:
1. Правильность `BOT_TOKEN`
2. Логи: `tail -f bot.log`
3. Интернет-соединение сервера

### Q: LLM-чат не работает

**A:** Убедитесь, что:
1. `ANTHROPIC_API_KEY` установлен корректно
2. На счету Anthropic есть средства
3. Вы являетесь админом (в `ADMIN_IDS`)

### Q: Автоторговля не работает

**A:** Проверьте:
1. `TG_API_ID` и `TG_API_HASH` заданы
2. `TARGET_CHAT_ID` содержит правильные ID каналов
3. Telethon установлен: `pip install telethon`

### Q: База данных недоступна

**A:** Проверьте:
1. `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY`
2. Интернет-соединение
3. Статус Supabase проекта

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи: `/logs`
2. Убедитесь, что все переменные окружения заданы
3. Перезапустите бота
4. Обратитесь в поддержку: @banana_pwr

---

## 🎯 Следующие шаги

После успешного деплоя:

1. ✅ Проверьте работу всех команд
2. ✅ Настройте webhook для Pocket Option (если нужно)
3. ✅ Добавьте тестовых пользователей
4. ✅ Протестируйте автоторговлю на demo-счете
5. ✅ Настройте мониторинг и алерты

---

**Удачного деплоя! 🚀**
